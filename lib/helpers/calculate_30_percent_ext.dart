import '../utils/preferences.dart';

extension Calculate30PercentExt on double {
 // double get percent30 {
 //   return double.tryParse(((this * Preferences.getInitialPaymentPercentage()) / 100).toStringAsFixed(2)) ?? 0;
 //   // return double.tryParse(((this * 30) / 100).toStringAsFixed(2)) ?? 0;
 // }
}